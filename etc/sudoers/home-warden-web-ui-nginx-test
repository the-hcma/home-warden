# Allow the unprivileged home-warden API process to run the read-only preview
# validator as root without a password prompt. Installed by scripts/setup-service.
@@OWNER@@ ALL=(root) NOPASSWD: @@REPO_DIR@@/scripts/nginx-test-candidate *

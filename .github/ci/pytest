#!/usr/bin/env bash
# ci/pytest — run the Python test suite via uv. Mirrors the-hcma/domesti-bot's
# .github/ci/pytest-hermetic, scaled down (no xdist/browser markers needed yet
# for this repo's much smaller Python surface).
set -euo pipefail
bash .github/ci/setup-python
uv run pytest
